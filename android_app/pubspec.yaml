name: line_oa_chat_hub
description: Android client for OPPO LINE OA Chat Hub AI.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ^3.4.0

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.15.2
  firebase_messaging: ^15.2.10
  flutter_secure_storage: ^9.2.4
  http: ^1.2.2
  connectivity_plus: ^6.1.0
  image_picker: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
